package ir.xweb.module;

import org.junit.Test;

import static org.mockito.Mockito.*;
import java.io.IOException;

public class TestWiki {

    @Test
    public void convertDir() throws IOException {
        /*final WikiModule wiki = new WikiModule(mock(Manager.class), mock(ModuleInfo.class), mock(ModuleParam.class));
        System.out.println(wiki.markdownToHtml("" +
                "# this is title\n" +
                "this is some text" +
                ""));*/
    }

}
